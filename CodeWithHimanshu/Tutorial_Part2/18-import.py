import mainn

mainn.enumlist([5, 6, 2, 7, 1])  # if this function was called and there was not if name = main cond,
                                    # every executable in that line had got executed.
